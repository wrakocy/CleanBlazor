<MudNumericField T="decimal?"
                 Label="@Label"
                 Value="@Value"
                 ValueChanged="@OnValueChanged"
                 For="@For"
                 Immediate="@false"
                 ShrinkLabel="@true"
                 Disabled="@Disabled"
                 Min="@Min" 
                 Max="@Max" 
                 Step="@Step" 
                 Format="0.00"
                 Pattern="^[0-9.]+$"
                 HelperText="@HelperText"
                 Adornment="Adornment.Start"
                 AdornmentIcon="@AppConstants.Icons.CurrencyDollar" />